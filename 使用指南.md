# AI-For-Beginners 使用指南

本指南包含项目启动、配置和使用的完整说明。

## 📋 目录

1. [快速开始](#快速开始)
2. [环境配置](#环境配置)
3. [启动方式](#启动方式)
4. [Jupyter Notebook 集成](#jupyter-notebook-集成)
5. [数据文件准备](#数据文件准备)
6. [常见问题](#常见问题)

---

## 🚀 快速开始

### 前提条件

- **操作系统**: Windows 10/11, macOS 10.15+, Ubuntu 18.04+
- **内存**: 8GB RAM（推荐 16GB）
- **磁盘空间**: 至少 20GB 可用空间（推荐 50GB）
- **网络**: 需要互联网连接以下载依赖和数据

### 一键启动（推荐）

```bash
./start_with_auto_token.sh
```

这个脚本会：
1. 检查并启动 Jupyter Lab（如果未运行）
2. 启动 Token 自动获取服务
3. 启动文档服务
4. 自动配置，让您可以直接点击 Notebook 链接

### 手动启动

#### 步骤 1: 激活环境

```bash
# 使用 conda
conda activate ai4beg

# 或使用虚拟环境
source ~/ai4beg_env/bin/activate
```

#### 步骤 2: 启动 Jupyter Lab

```bash
jupyter lab
```

#### 步骤 3: 启动 Token 服务（可选，用于自动获取 token）

```bash
python3 jupyter_token_server.py
```

#### 步骤 4: 打开文档

在浏览器中打开 `index.html` 或访问文档服务地址。

---

## 🔧 环境配置

### 安装 Miniconda（推荐）

#### macOS/Linux

```bash
# 使用 Homebrew (macOS)
brew install miniconda

# 或下载安装包
wget https://repo.anaconda.com/miniconda/Miniconda3-latest-Linux-x86_64.sh
bash Miniconda3-latest-Linux-x86_64.sh
```

#### Windows

1. 访问：https://docs.conda.io/en/latest/miniconda.html
2. 下载 Windows 64-bit 安装程序
3. 安装时选择 "Add Miniconda3 to PATH"

### 创建项目环境

```bash
# 克隆项目
git clone https://github.com/microsoft/AI-For-Beginners.git
cd AI-For-Beginners

# 创建环境
conda env create --name ai4beg --file environment.yml

# 激活环境
conda activate ai4beg
```

### 验证安装

```bash
python -c "import torch; print('PyTorch:', torch.__version__)"
python -c "import tensorflow as tf; print('TensorFlow:', tf.__version__)"
python -c "import jupyter; print('Jupyter installed')"
```

---

## 🎯 启动方式

### 方式一：Jupyter Lab（推荐）⭐

```bash
conda activate ai4beg
jupyter lab
```

访问：`http://localhost:8888/lab`

**优势**：
- 更现代的界面
- 支持多标签页
- 更好的文件管理
- 集成终端

### 方式二：Jupyter Notebook（经典界面）

```bash
conda activate ai4beg
jupyter notebook
```

访问：`http://localhost:8888`

### 方式三：VS Code

1. 安装 VS Code 和 Python 扩展
2. 打开项目：`code .`
3. 选择 Python 解释器：`Ctrl+Shift+P` → "Python: Select Interpreter" → 选择 `ai4beg` 环境
4. 打开 `.ipynb` 文件即可直接运行

### 方式四：Docker/Dev Container

```bash
# 在 VS Code 中
# 1. 安装 Dev Containers 扩展
# 2. 按 F1 → "Dev Containers: Reopen in Container"
```

---

## 📓 Jupyter Notebook 集成

### 自动 Token 配置

项目支持自动获取 Jupyter token，无需手动配置：

1. **启动 Token 服务**：
   ```bash
   python3 jupyter_token_server.py
   ```

2. **打开文档页面**：
   - 文档会自动从 Token 服务获取 token
   - 点击 Notebook 链接即可直接打开，无需输入密码

### 手动配置 Token

如果自动配置失败，可以手动设置：

#### 方法 1: 获取 Token

```bash
jupyter server list
```

复制输出中的 token（`token=` 后面的字符串）

#### 方法 2: 在浏览器中设置

打开浏览器控制台（F12），输入：

```javascript
localStorage.setItem('jupyter_token', 'YOUR_TOKEN');
location.reload();
```

#### 方法 3: 配置密码（推荐）

```bash
jupyter server password
```

设置密码后，可以直接使用密码登录。

### 配置 Jupyter 服务器地址

如果需要连接到远程 Jupyter 服务器，编辑 `index.html`：

```javascript
window.$docsify = {
  jupyterUrl: 'http://localhost:8888',  // 修改为您的服务器地址
  jupyterMode: 'lab',  // 'lab' 或 'notebook'
}
```

---

## 📦 数据文件准备

### 自动下载数据文件

运行下载脚本：

```bash
bash download_missing_data.sh
```

脚本会自动下载：
- ✅ `mnist.pkl.gz` - MNIST 数据集（用于感知机和自定义框架实验）
- ✅ `kagglecatsanddogs_5340.zip` - 猫狗数据集（用于迁移学习，约 786MB）

### 需要手动下载的文件

#### NER 数据集

- **文件**: `ner_dataset.csv`
- **位置**: `lessons_zh/5-NLP/19-NER/ner_dataset.csv`
- **下载地址**: https://www.kaggle.com/datasets/abhinavwalia95/entity-annotated-corpus
- **说明**: 需要 Kaggle 账号登录

**下载步骤**:
1. 访问下载地址
2. 点击 "Download" 按钮（需要登录）
3. 解压下载的 zip 文件
4. 找到 `ner_dataset.csv` 文件
5. 复制到 `lessons_zh/5-NLP/19-NER/` 目录

### 自动下载的数据文件

以下文件在对应的 notebook 中已包含自动下载代码，运行时会自动下载：

- **Oxford-IIIT Pet Dataset**: `PetFaces.ipynb` 中包含下载代码
- **oxcats.tar.gz**: `Clip.ipynb` 中包含下载代码
- **onto.ttl**: 已存在于 `2-Symbolic/data/onto.ttl`

详细说明请查看 `数据文件说明.md`。

---

## ❓ 常见问题

### 问题 1: Conda 命令未找到

**解决方案**:

**macOS/Linux**:
```bash
conda init zsh  # 或 bash
source ~/.zshrc  # 或 ~/.bashrc
```

**Windows**:
- 重新安装 Miniconda，确保勾选 "Add to PATH"
- 或手动添加到 PATH

### 问题 2: 环境创建失败（SSL 错误）

**解决方案**:

```bash
# 使用自动修复脚本
./fix-conda-ssl.sh

# 或使用 pip 安装
./setup-with-pip.sh
```

### 问题 3: Jupyter 无法启动

**解决方案**:

```bash
# 确保环境已激活
conda activate ai4beg

# 检查 Jupyter 是否安装
jupyter --version

# 重新安装
conda install jupyter jupyterlab

# 使用其他端口
jupyter lab --port=8889
```

### 问题 4: 点击 Notebook 链接需要输入密码

**解决方案**:

1. **使用自动 Token 服务**（推荐）：
   ```bash
   python3 jupyter_token_server.py
   ```

2. **手动设置 Token**：
   - 运行 `jupyter server list` 获取 token
   - 在浏览器控制台设置：`localStorage.setItem('jupyter_token', 'YOUR_TOKEN')`

3. **配置密码**：
   ```bash
   jupyter server password
   ```

### 问题 5: 无法导入 PyTorch/TensorFlow

**解决方案**:

```bash
# 检查环境
conda activate ai4beg
python -c "import sys; print(sys.executable)"

# 重新安装
conda install pytorch torchvision -c pytorch
pip install tensorflow
```

### 问题 6: 内存不足

**解决方案**:

1. 关闭其他应用
2. 减少批次大小（在 Notebook 中）
3. 使用数据生成器
4. 清理变量：`del large_variable; import gc; gc.collect()`

### 问题 7: 中文显示乱码

**解决方案**:

在 Notebook 中设置：

```python
import matplotlib.pyplot as plt
plt.rcParams['font.sans-serif'] = ['SimHei', 'Arial Unicode MS', 'DejaVu Sans']
plt.rcParams['axes.unicode_minus'] = False
```

---

## 📚 学习路径建议

1. **从基础开始**
   - 完成 `lessons/0-course-setup/` 的设置
   - 阅读 `lessons/1-Intro/README.md`

2. **按顺序学习**
   - 按照课程编号顺序学习
   - 每个模块先读 README，再执行 Notebook

3. **实践为主**
   - 不要只看代码，要实际运行
   - 尝试修改参数，观察结果变化
   - 完成 lab 和 assignment

---

## 🔗 相关资源

- **官方文档**: https://microsoft.github.io/AI-For-Beginners/
- **GitHub 仓库**: https://github.com/microsoft/ai-for-beginners
- **数据文件说明**: 查看 `数据文件说明.md`
- **Notebook 检查报告**: 查看 `notebook_check_summary.md`

---

## 📝 快速命令参考

```bash
# 激活环境
conda activate ai4beg

# 启动 Jupyter Lab
jupyter lab

# 启动 Token 服务
python3 jupyter_token_server.py

# 一键启动（推荐）
./start_with_auto_token.sh

# 下载数据文件
bash download_missing_data.sh

# 检查 Jupyter 服务器
jupyter server list

# 设置密码
jupyter server password
```

---

祝学习愉快！🎓

